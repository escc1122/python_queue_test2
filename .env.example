# ========================================
# 環境變數範例
# 實際部署時,請從 deploy/envs/ 目錄複製對應的配置檔案
# ========================================

# 啟動的服務 (用逗號分隔)
# 可選值: redis, email, data, report
COMPOSE_PROFILES=redis,email,data,report

# Context 配置
CONTEXT_NAME=local
LOG_DIR=./logs

# Redis 配置
REDIS_HOST=localhost
REDIS_PORT=6380
REDIS_DB=0
REDIS_PASSWORD=your_password_here
REDIS_EXPOSE=127.0.0.1

# ========================================
# 快速開始:
# 
# 單機測試:
#   cp deploy/envs/.env.local .env
#   docker-compose up -d
# 
# 多機部署:
#   Redis 機器:   cp deploy/envs/.env.redis .env
#   Email 機器:   cp deploy/envs/.env.email .env
#   Data 機器:    cp deploy/envs/.env.data .env
#   Report 機器:  cp deploy/envs/.env.report .env
# ========================================
